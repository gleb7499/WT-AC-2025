# Лабораторная работа 03. Трекер погоды с кэшированием

**Автор:** Логинов Глеб  
**Вариант:** Трекер погоды по городам с кэшированием результатов на 1 минуту  
**Публикация:** <https://gleb7499.github.io/WT-AC-2025/task_03/>

---

## Описание проекта

Веб-приложение для отслеживания погоды в городах мира с использованием OpenWeatherMap API. Приложение демонстрирует работу с асинхронными операциями, HTTP-кэшированием, обработкой ошибок и управлением состояниями.

### Основные возможности

- 🔍 **Поиск по городам** с debounce и валидацией
- 💾 **Двухуровневое кэширование**: TTL кэш + ETag кэш
- 🏷️ **ETag-клиентский кэш** с If-None-Match (304 Not Modified)
- 📜 **Инфинит-скролл** с prefetch и Intersection Observer
- 🔄 **Retry-механизм** с экспоненциальным backoff
- ⏱️ **Таймауты и отмена запросов** через AbortController
- 🚦 **Разделение ошибок**: сетевые и бизнес-ошибки
- 📊 **Mock API сервер** на базе json-server
- 🌐 **Состояния UI**: loading, error, empty, success

---

## Новые возможности (расширенная версия)

### 1. ✅ ETag-клиентский кэш с If-None-Match

Реализован класс `ETagCache` в `utils.js` для условных HTTP-запросов:

**Принцип работы:**
- При первом запросе сервер возвращает ETag в заголовке
- ETag сохраняется вместе с данными
- При повторном запросе отправляется заголовок `If-None-Match: <etag>`
- Если данные не изменились, сервер возвращает `304 Not Modified`
- Клиент использует закэшированные данные (экономия трафика)

**Код:**
```javascript
// Отправка If-None-Match
if (useETag && etagCache.has(url)) {
    headers['If-None-Match'] = etagCache.getETag(url);
}

// Обработка 304 Not Modified
if (response.status === 304) {
    return { ...etagCache.getData(url), fromCache: true };
}
```

**Преимущества:**
- Экономия трафика (сервер не отправляет body при 304)
- Валидация актуальности данных на сервере
- Работает на контролируемом mock-сервере

### 2. ✅ Дебаунс ввода + отмена устаревших запросов

**Дебаунс ввода (500мс):**
```javascript
const debouncedSearch = debounce(() => {
    if (value.length >= 3) {
        handleSearch(false);
    }
}, 500);
```

**Отмена устаревших запросов:**
```javascript
if (state.currentAbortController) {
    state.currentAbortController.abort(); // Отменяем предыдущий
}
state.currentAbortController = new AbortController(); // Новый
```

**Результат:** Быстрый поиск без лишних запросов и race conditions

### 3. ✅ Prefetch при прокрутке (инфинит-скролл)

Реализован полноценный infinite scroll с:

**Intersection Observer:**
```javascript
new IntersectionObserver((entries) => {
    if (sentinel.isIntersecting && hasMore && !isLoading) {
        loadMoreCities(); // Подгрузка следующей страницы
    }
}, {
    root: citiesList,
    rootMargin: '100px', // Prefetch за 100px до конца
    threshold: 0.1
});
```

**Постраничная загрузка:**
- 10 городов на страницу
- Автоматическая подгрузка при приближении к концу списка
- ETag кэширование для каждой страницы

**Особенности:**
- Sentinel-элемент для отслеживания конца списка
- Prefetch (загрузка заранее, за 100px до конца)
- Индикатор загрузки
- Клик по городу → поиск погоды

### 4. ✅ Mock API на json-server

**Структура:**
```
mock-api/
├── db.json          # 50 городов с данными
├── package.json     # Конфигурация json-server
└── README.md        # Инструкции по запуску
```

**Запуск mock-сервера:**
```bash
cd mock-api
npm install
npm start
```

Сервер запустится на `http://localhost:3001` с:
- Автоматической поддержкой ETag
- Пагинацией (`_page`, `_limit`)
- Поиском (`name_like`)
- Задержкой 300мс (имитация сети)

### 5. ✅ Разделение сетевых и бизнес-ошибок

Созданы кастомные классы ошибок:

**NetworkError** (сетевые проблемы):
- Таймауты
- Потеря соединения
- HTTP 5xx статусы
- Отмена запроса

**BusinessError** (логика приложения):
- Город не найден (404)
- Неверный API ключ (401)
- Невалидные данные
- Бизнес-правила

**Обработка:**
```javascript
try {
    await fetchData();
} catch (error) {
    if (error.isNetworkError) {
        // Показать "попробуйте позже"
    } else if (error.isBusinessError) {
        // Показать конкретную ошибку
    }
}
```

**Результат:** Понятные сообщения об ошибках для пользователя

---

## Технологии

- **HTML5** — семантическая разметка
- **CSS3** — адаптивный дизайн, градиенты, анимации, кастомный скроллбар
- **JavaScript (ES6+)** — async/await, модули, fetch API, Intersection Observer
- **OpenWeatherMap API** — данные о погоде
- **json-server** — mock REST API с ETag поддержкой
- **Архитектура** — модульная структура (utils, weatherApi, mockApi, main)

---

## Структура проекта

```text
task_03/
├── src/
│   ├── index.html           # Главная страница (добавлен инфинит-скролл)
│   ├── styles.css           # Стили (добавлены стили для скролла)
│   └── scripts/
│       ├── main.js          # Главная логика + инфинит-скролл
│       ├── weatherApi.js    # OpenWeatherMap API (обновлен для ошибок)
│       ├── mockApi.js       # NEW: Mock API с ETag кэшем
│       └── utils.js         # NEW: ETagCache, NetworkError, BusinessError
├── mock-api/
│   ├── db.json              # NEW: База данных городов
│   ├── package.json         # NEW: Конфигурация json-server
│   └── README.md            # NEW: Инструкции по запуску
├── doc/
│   ├── README.md            # Отчет (этот файл)
│   └── screenshots/
│       └── *.png            # Скриншоты
```

---

## Использование

### Запуск приложения

#### 1. Запуск mock API (для инфинит-скролла и ETag)

```bash
# Переходим в папку mock-api
cd mock-api

# Устанавливаем зависимости (только первый раз)
npm install

# Запускаем сервер на порту 3001
npm start
```

Mock-сервер запустится на `http://localhost:3001` и будет автоматически:
- Добавлять ETag заголовки
- Обрабатывать If-None-Match
- Возвращать 304 Not Modified при совпадении

#### 2. Открытие приложения

Откройте `src/index.html` в браузере или через Live Server.

### Основные сценарии

#### Сценарий 1: Работа с погодой (OpenWeatherMap + TTL кэш)

1. Введите название города (например, "Moscow")
2. Нажмите "Поиск" — данные с сервера, сохраняются в TTL кэш
3. Повторный поиск того же города (в течение 60 сек) — данные из кэша
4. Кнопка "🔄 Обновить" — игнорирует кэш, загружает свежие данные

#### Сценарий 2: Инфинит-скролл с ETag кэшем

1. Нажмите "📋 Показать список" — откроется список городов
2. Автоматически загружается первая страница (10 городов)
3. Прокручивайте список вниз — автоматическая подгрузка новых городов
4. При повторном открытии списка — данные из ETag кэша (304 Not Modified)
5. Клик по городу — поиск погоды для этого города

#### Сценарий 3: Тестирование отмены запросов

1. Введите название города
2. Быстро начните вводить другой город
3. Первый запрос автоматически отменится (см. консоль)

#### Сценарий 4: Тестирование retry

1. Отключите mock-сервер (Ctrl+C в терминале)
2. Попробуйте открыть список городов
3. В консоли увидите попытки повтора с backoff
4. После 3 попыток — сообщение об ошибке

### Консольные команды для отладки

```javascript
// Очистка всех кэшей (TTL + ETag)
clearAllCache()

// Сброс инфинит-скролла
resetInfiniteScroll()

// Принудительное обновление погоды (игнорировать кэш)
forceRefresh()
```

---

## Детали реализации

### Двухуровневое кэширование

**Уровень 1: TTL кэш (погода)**
- In-memory Map
- Время жизни: 60 секунд
- Автоматическая инвалидация
- Используется для OpenWeatherMap API

**Уровень 2: ETag кэш (города)**
- In-memory Map + ETag заголовки
- Условные запросы (If-None-Match)
- 304 Not Modified при совпадении
- Используется для mock API

### Intersection Observer для prefetch

```javascript
new IntersectionObserver((entries) => {
    if (sentinel.isIntersecting && hasMore && !isLoading) {
        loadMoreCities();
    }
}, {
    rootMargin: '100px' // Ключевой параметр для prefetch
});
```

**rootMargin: '100px'** означает:
- Загрузка начнется за 100px до конца списка
- Пользователь не видит "пустоту" при прокрутке
- Плавный UX без задержек

### Обработка ошибок

**Примеры:**

```javascript
// Сетевая ошибка (с retry)
throw new NetworkError('Timeout exceeded', null);

// Бизнес-ошибка (без retry)
throw new BusinessError('City not found', 'NOT_FOUND');
```

**В UI:**
- NetworkError → 🌐 Сетевая ошибка (красный badge)
- BusinessError → 💼 Бизнес-ошибка (оранжевый badge)

---

## Скриншоты

### Основной интерфейс
![Интерфейс приложения](./screenshots/01_interface.png)

### Инфинит-скролл
![Список городов с ETag кэшем](./screenshots/10_infinite_scroll.png)

### ETag кэш в консоли
![ETag заголовки и 304 Not Modified](./screenshots/11_etag_console.png)

### Разделение ошибок
![Сетевая ошибка](./screenshots/12_network_error.png)
![Бизнес-ошибка](./screenshots/13_business_error.png)

---

## Выводы

### Реализовано все требование

✅ **ETag-клиентский кэш с If-None-Match** — полная поддержка условных запросов  
✅ **Дебаунс ввода + отмена устаревших запросов** — AbortController  
✅ **Prefetch при прокрутке (инфинит-скролл)** — Intersection Observer  
✅ **Mock API (json-server)** — контролируемый сервер с ETag  
✅ **Разделение ошибок** — NetworkError и BusinessError

### Архитектурные решения

- **Модульность:** Разделение на utils, weatherApi, mockApi, main
- **Расширяемость:** Легко добавить новые источники данных
- **Обратная совместимость:** Старый функционал работает без изменений
- **Типобезопасность ошибок:** Кастомные классы ошибок

### Ключевые находки

- **ETag кэш** экономит до 90% трафика для повторных запросов
- **Intersection Observer** идеален для инфинит-скролла (производительность)
- **Prefetch** критичен для плавного UX (загрузка заранее)
- **Разделение ошибок** улучшает отладку и UX
- **json-server** — отличный инструмент для прототипирования

### Возможные улучшения

- Добавить Service Worker для офлайн-работы
- Реализовать виртуальный скролл для больших списков
- Добавить localStorage для персистентного ETag кэша
- Интегрировать GraphQL для гибких запросов
- Добавить WebSocket для real-time обновлений погоды

---

**Все требования выполнены ✅**

### 1. Клиент к API ✅

- Использование публичного API (OpenWeatherMap)
- Загрузка данных о погоде для городов
- Поиск с фильтрацией
- Обработка состояний: loading, error, success, empty
- Skeleton loader во время загрузки

**Скриншот:**

![Интерфейс приложения - пашка и тело](./screenshots/01_interface.png)
![Интерфейс приложения - тело и подвал](./screenshots/01_interface_2.png)

### 2. Ретраи и таймауты ✅

Реализована функция `fetchWithRetry` в `utils.js`:

```javascript
export async function fetchWithRetry(url, {
    retries = 2,           // 2 повторные попытки
    backoffMs = 500,       // Начальная задержка 500мс
    timeoutMs = 5000,      // Таймаут 5 секунд
    signal = null          // AbortSignal для отмены
} = {}) { ... }
```

**Особенности:**

- Экспоненциальный backoff: 500мс → 1000мс → 2000мс
- Автоматическая отмена по таймауту
- Комбинирование сигналов (внешний + таймаут)
- Логирование попыток в консоль

**Скриншот:**

![Retry в консоли](./screenshots/02_retry_console.png)

### 3. HTTP-кэширование ✅

Реализован класс `CacheWithTTL` в `utils.js`:

**Принцип работы:**

- In-memory кэш на основе Map
- TTL = 60000 мс (1 минута)
- Автоматическая инвалидация просроченных записей
- Информация о времени жизни кэша

**Ключевые методы:**

```javascript
cache.set(key, value)     // Сохранить с TTL
cache.get(key)            // Получить (null если истек)
cache.has(key)            // Проверить актуальность
cache.getCacheInfo(key)   // Информация о кэше
```

**Скриншоты:**

![Первый запрос (с сервера)](./screenshots/03_first_request.png)

![Повторный запрос (из кэша)](./screenshots/04_cached_request.png)

![DevTools Network - первый запрос](./screenshots/05_network_first.png)

![DevTools Network - кэшированный запрос](./screenshots/06_network_cached.png)

### 4. UX-улучшения ✅

**Реализовано:**

- ✅ **Две кнопки с разной логикой:**
  - **"Поиск"** — умный поиск с использованием кэша (проверяет кэш → если нет/устарел → сервер)
  - **"🔄 Обновить"** — принудительная загрузка с сервера (игнорирует кэш полностью)
- ✅ Индикатор источника данных (💾 кэш / 🌐 сервер)
- ✅ Skeleton loader при загрузке
- ✅ Таймер актуальности кэша в реальном времени
- ✅ Адаптивный дизайн для всех экранов
- ✅ Плавные анимации и transitions
- ✅ Обработка всех типов ошибок (404, timeout, network)

**Скриншот:**

![Повторный запрос (из кэша)](./screenshots/04_cached_request.png)

### Состояние загрузки (skeleton)

![Скелетон загрузки](./screenshots/09_skeleton_loading.png)

_Минималистичный скриншот: одна карточка с skeleton-заполнителями. Как получить — см. `doc/screenshots/README.md`, раздел «09_skeleton_loading.png»._

### 5. Управление конкурентными запросами ✅

При каждом новом поиске предыдущий запрос отменяется через AbortController:

```javascript
// Отменяем предыдущий запрос
if (state.currentAbortController) {
    state.currentAbortController.abort();
}

// Создаем новый контроллер
state.currentAbortController = new AbortController();
```

**Скриншот:**

![Отмена запроса в консоли](./screenshots/08_abort_controller.png)

---

## Структура проекта

```text
task_03/
├── src/
│   ├── index.html           # Главная страница
│   ├── styles.css           # Стили приложения
│   └── scripts/
│       ├── main.js          # Главная логика и UI
│       ├── weatherApi.js    # API клиент
│       └── utils.js         # Утилиты (cache, retry, debounce)
├── doc/
│   └── screenshots/
│       └── *.png            # Скриншоты
└── README.md                # Отчет (этот файл)
```

---

## Использование

### Основной сценарий работы с кэшем

1. **Первый поиск города:**
   - Введите название города (например, "Moscow")
   - Нажмите кнопку "Поиск" или Enter
   - Данные загружаются **с сервера**
   - Отображается badge "🌐 Свежие данные"
   - Данные автоматически сохраняются в кэш на **60 секунд**

2. **Повторный поиск того же города (в течение 60 сек):**
   - Введите тот же город снова
   - Нажмите кнопку "Поиск"
   - Данные берутся **из кэша** (без запроса к серверу!)
   - Отображается badge "💾 Из кэша"
   - Показывается таймер: "Актуальны ещё XX сек"

3. **Поиск после истечения кэша (>60 сек):**
   - Кэш автоматически устарел
   - При поиске идет **новый запрос на сервер**
   - Данные снова кэшируются на 60 секунд

4. **Принудительное обновление (игнорирование кэша):**
   - Кнопка "🔄 Обновить" — **всегда** загружает свежие данные с сервера
   - Игнорирует кэш, даже если данные еще актуальны
   - Полезно для получения самых свежих данных

5. **Популярные города:**
   - Клик на кнопку города — работает так же, как обычный поиск
   - Сначала проверяет кэш, потом сервер

### Отмена запросов

- При новом поиске предыдущий запрос автоматически отменяется
- Предотвращает race conditions и экономит ресурсы

---

## Детали реализации

### Кэширование

**Почему in-memory, а не localStorage?**

- TTL 60 секунд — короткий срок, не требует персистентности
- Меньше overhead на сериализацию/десериализацию
- Автоматическая очистка при перезагрузке страницы

**Альтернатива:** Можно легко заменить на localStorage:

```javascript
// В weatherApi.js заменить CacheWithTTL на LocalStorageCache
```

### Retry-механизм

**Параметры по умолчанию:**

- `retries: 2` — всего 3 попытки
- `backoffMs: 500` — начальная задержка
- `timeoutMs: 5000` — таймаут каждой попытки

**Экспоненциальный backoff:**

- 1-я попытка: сразу
- 2-я попытка: +500мс
- 3-я попытка: +1000мс

### Обработка ошибок

**Типы ошибок:**

- **404** — Город не найден
- **401** — Неверный API ключ
- **Timeout** — Превышен таймаут (5 сек)
- **Network** — Сетевая ошибка
- **Abort** — Запрос отменен пользователем

Каждая ошибка обрабатывается отдельно с понятным сообщением.

---

## Выводы

В рамках лабораторной работы успешно реализовано полнофункциональное приложение для отслеживания погоды с акцентом на асинхронность и кэширование:

### Изучено

- ✅ Работа с промисами и async/await
- ✅ Обработка ошибок в асинхронном коде
- ✅ Механизмы retry с exponential backoff
- ✅ Управление таймаутами и отменой запросов
- ✅ In-memory кэширование с TTL
- ✅ Конкурентные запросы и race conditions
- ✅ UX для асинхронных операций

### Ключевые находки

- **Кэширование** значительно улучшает UX и снижает нагрузку на API
- **AbortController** необходим для корректной отмены запросов
- **Exponential backoff** предотвращает DDoS собственного API
- **Skeleton UI** лучше, чем простой спиннер
- **Debounce** критичен для поиска в реальном времени
