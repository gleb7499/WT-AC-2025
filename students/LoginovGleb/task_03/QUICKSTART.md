# Инструкция по запуску проекта

## Быстрый старт

### 1. Запуск Mock API сервера (для инфинит-скролла с ETag)

```bash
# Переходим в папку mock-api
cd mock-api

# Устанавливаем зависимости (только первый раз)
npm install

# Запускаем сервер
npm start
```

Mock-сервер запустится на `http://localhost:3001`

**Что делает mock-сервер:**
- Предоставляет список из 50 городов
- Поддерживает пагинацию и поиск
- Автоматически добавляет ETag заголовки
- Возвращает 304 Not Modified при совпадении ETag

### 2. Открытие приложения

Откройте файл `src/index.html` в браузере:
- Двойной клик на файле, или
- Через Live Server в VS Code, или
- Просто перетащите файл в браузер

### 3. Использование приложения

#### Поиск погоды (OpenWeatherMap API)
1. Введите название города в поле поиска
2. Нажмите "Поиск" или Enter
3. Данные кэшируются на 60 секунд
4. Кнопка "🔄 Обновить" игнорирует кэш

#### Инфинит-скролл (Mock API с ETag)
1. Нажмите "📋 Показать список"
2. Прокручивайте вниз — автоматическая подгрузка
3. Клик по городу → поиск погоды
4. При повторном открытии — данные из ETag кэша

## Что реализовано

✅ **ETag-клиентский кэш с If-None-Match** (304 Not Modified)  
✅ **Дебаунс ввода + отмена устаревших запросов** (AbortController)  
✅ **Prefetch при прокрутке** (Intersection Observer + infinite scroll)  
✅ **Mock API на json-server** (контролируемый сервер с ETag)  
✅ **Разделение ошибок** (NetworkError vs BusinessError)

## Отладка

### Консольные команды

```javascript
// Очистить все кэши (TTL + ETag)
clearAllCache()

// Сброс инфинит-скролла
resetInfiniteScroll()

// Принудительное обновление погоды
forceRefresh()
```

### Просмотр логов

Откройте DevTools (F12) → Console:
- Запросы к API
- Состояние кэшей
- ETag заголовки
- Отмена запросов

### Просмотр сетевых запросов

DevTools (F12) → Network:
- Первый запрос: 200 OK с данными
- Повторный запрос: 304 Not Modified без body
- If-None-Match заголовок в запросе
- ETag заголовок в ответе

## Структура файлов

```
task_03/
├── src/
│   ├── index.html              # UI приложения
│   ├── styles.css              # Стили
│   └── scripts/
│       ├── main.js             # Главная логика + инфинит-скролл
│       ├── weatherApi.js       # OpenWeatherMap API клиент
│       ├── mockApi.js          # Mock API клиент с ETag
│       └── utils.js            # ETagCache, NetworkError, etc.
├── mock-api/
│   ├── db.json                 # База данных городов (50 шт)
│   ├── package.json            # Конфигурация json-server
│   └── README.md               # Документация mock API
└── doc/
    ├── README.md               # Полный отчет
    └── screenshots/            # Скриншоты

## Требования

- Node.js 14+ (для json-server)
- Современный браузер с поддержкой ES6+ модулей
- Интернет (для OpenWeatherMap API)

## Возможные проблемы

### Mock-сервер не запускается

**Решение:**
```bash
cd mock-api
rm -rf node_modules package-lock.json
npm install
npm start
```

### CORS ошибки

**Решение:** json-server автоматически настраивает CORS, но если проблема:
- Используйте Live Server в VS Code
- Или расширение "Allow CORS" в браузере

### Модули не загружаются

**Ошибка:** "Failed to load module script"

**Решение:**
- Откройте через HTTP сервер (Live Server)
- Не открывайте напрямую через `file://`

## Дополнительная информация

- Полная документация: `doc/README.md`
- Документация mock API: `mock-api/README.md`
- Исходный код: `src/scripts/`
